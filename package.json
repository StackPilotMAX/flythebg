{
  "name": "flythebg",
  "private": true,
  "version": "2.0.0",
  "description": "FlyThe BG - privacy-first browser media tools with a Cloudflare/Hugging Face background remover",
  "scripts": {
    "build": "tsc && node scripts/prepare-public.mjs && cp worker.js public/worker.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
